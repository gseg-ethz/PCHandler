# Throwaway observation file — Phase 13 plan 13-10 Task 1

This file exists only to carry a trivial, non-release-artifact change on a pull
request into `main`, so that the `Integrity (base-ref)` check run can be observed
reporting against the pull request's head commit before the ruleset payload that
requires that context is applied.

The pull request carrying this file is closed **without merging**. If you are
reading this on a branch that was merged, that is a mistake — delete the file.

Recorded 2026-08-03, Phase 13 plan 13-10.
